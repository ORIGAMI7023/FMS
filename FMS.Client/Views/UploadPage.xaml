<Window x:Class="FMS.Client.Views.UploadPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="上传营收数据" Height="550" Width="650">
    <Grid Margin="10">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Label Content="日期" Width="50"/>
                <DatePicker SelectedDate="{Binding Date}" Width="150"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Label Content="备注" Width="50"/>
                <TextBox Text="{Binding Remark}" Width="400"/>
            </StackPanel>
            <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" Height="350" Margin="0,0,0,10" CanUserAddRows="True">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="医生" Width="*">
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding AllDoctors}"
                                          SelectedItem="{Binding Doctor, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="分类" Width="*">
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding Categories}"
                                          SelectedItem="{Binding Category}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="金额" Binding="{Binding Amount}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="上传" Command="{Binding UploadCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>